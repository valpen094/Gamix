<UserControl x:Class="Gamix.UI.Views.Components.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gamix.UI.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="400">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Brush_AccentPrimary}"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20,15,20,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/> <!-- Icon -->
            <ColumnDefinition Width="Auto"/> <!-- Title -->
            <ColumnDefinition Width="*"/>    <!-- Spacer -->
            <ColumnDefinition Width="Auto"/> <!-- Preset Input -->
            <ColumnDefinition Width="Auto"/> <!-- Theme Palette -->
            <ColumnDefinition Width="Auto"/> <!-- Minimize -->
            <ColumnDefinition Width="Auto"/> <!-- Close -->
        </Grid.ColumnDefinitions>

        <!-- App Icon (Sidebar Toggle Button) -->
        <Button Grid.Column="0" FontSize="24" 
                VerticalAlignment="Center" Margin="0,0,8,0"
                Background="Transparent" BorderThickness="0"
                Cursor="Hand" ToolTip="メニューを開く"
                Click="ShowSidebar_Click">
             <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Rainbow Circle (Rotating Background) -->
                        <Ellipse x:Name="RainbowPulse" Width="40" Height="40" Opacity="0.8" RenderTransformOrigin="0.5,0.5" IsHitTestVisible="False">
                            <Ellipse.Fill>
                                <LinearGradientBrush x:Name="RainbowBrush" StartPoint="0,0" EndPoint="1,1">
                                    <LinearGradientBrush.Transform>
                                        <RotateTransform Angle="0" CenterX="0.5" CenterY="0.5"/>
                                    </LinearGradientBrush.Transform>
                                    <GradientStop Color="#FF5F6D" Offset="0.0"/>
                                    <GradientStop Color="#FFC371" Offset="0.2"/>
                                    <GradientStop Color="#FFEF96" Offset="0.4"/>
                                    <GradientStop Color="#50C878" Offset="0.6"/>
                                    <GradientStop Color="#4FACFE" Offset="0.8"/>
                                    <GradientStop Color="#A18CD1" Offset="1.0"/>
                                </LinearGradientBrush>
                            </Ellipse.Fill>
                        </Ellipse>
                        
                        <!-- Icon Content -->
                        <ContentPresenter x:Name="IconPresenter" Content="🎵" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="-1,-2,0,0"
                                          RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter.RenderTransform>
                                <ScaleTransform x:Name="IconTransform" ScaleX="1" ScaleY="1"/>
                            </ContentPresenter.RenderTransform>
                            <ContentPresenter.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource Brush_AccentPrimary}"/>
                                </Style>
                            </ContentPresenter.Resources>
                        </ContentPresenter>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <!-- Gentle Bounce (Scale) -->
                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="IconTransform"
                                                                   Storyboard.TargetProperty="ScaleX"
                                                                   RepeatBehavior="Forever">
                                        <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1.0"/>
                                        <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1.1">
                                             <EasingDoubleKeyFrame.EasingFunction>
                                                <SineEase EasingMode="EaseInOut"/>
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                        <EasingDoubleKeyFrame KeyTime="0:0:1.0" Value="1.0">
                                             <EasingDoubleKeyFrame.EasingFunction>
                                                <SineEase EasingMode="EaseInOut"/>
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                        <EasingDoubleKeyFrame KeyTime="0:0:2.0" Value="1.0"/> <!-- Pause -->
                                    </DoubleAnimationUsingKeyFrames>
                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="IconTransform"
                                                                   Storyboard.TargetProperty="ScaleY"
                                                                   RepeatBehavior="Forever">
                                        <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1.0"/>
                                        <EasingDoubleKeyFrame KeyTime="0:0:0.5" Value="1.1">
                                             <EasingDoubleKeyFrame.EasingFunction>
                                                <SineEase EasingMode="EaseInOut"/>
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                        <EasingDoubleKeyFrame KeyTime="0:0:1.0" Value="1.0">
                                             <EasingDoubleKeyFrame.EasingFunction>
                                                <SineEase EasingMode="EaseInOut"/>
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                        <EasingDoubleKeyFrame KeyTime="0:0:2.0" Value="1.0"/> <!-- Pause -->
                                    </DoubleAnimationUsingKeyFrames>
                        
                                    <!-- Rainbow Rotate (Continuous) -->
                                    <DoubleAnimation Storyboard.TargetName="RainbowBrush"
                                                     Storyboard.TargetProperty="(LinearGradientBrush.Transform).(RotateTransform.Angle)"
                                                     From="0" To="360" Duration="0:0:3" RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
        
        <!-- Title -->
        <TextBlock Grid.Column="1" Text="Gamix!" 
                   FontSize="28" FontWeight="Bold"
                   Foreground="{DynamicResource Brush_AccentPrimary}"
                   VerticalAlignment="Center"/>
        
        <!-- Preset Input -->
        <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
            <TextBox Width="100" Height="28" Margin="0,0,8,0" 
                     Text="{Binding NewPresetName, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center" Padding="8,0"
                     BorderThickness="1" BorderBrush="{DynamicResource Brush_AccentSecondary}">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="14"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            <Button Content="💾" Command="{Binding SavePresetCommand}"
                    Width="28" Height="28"
                    BorderThickness="0" FontSize="14"
                    Padding="0,0,0,2"
                    ToolTip="プリセットを保存">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{DynamicResource Brush_AccentTertiary}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="14">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Opacity" Value="0.8"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
        
        <!-- Palette Button -->
        <Button Grid.Column="4" Content="🎨"
                Width="28" Height="28" Margin="0,0,8,0"
                BorderThickness="0" FontSize="16"
                ToolTip="テーマを変更"
                Click="PaletteButton_Click"
                Padding="0,0,0,2">
            <Button.Resources>
                <ContextMenu x:Key="ThemeMenu" ItemsSource="{Binding AvailableThemes}" Placement="Bottom">
                    <ContextMenu.ItemContainerStyle>
                        <Style TargetType="MenuItem">
                            <Setter Property="Header" Value="{Binding}"/>
                            <Setter Property="Command" Value="{Binding DataContext.ChangeThemeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Setter Property="CommandParameter" Value="{Binding}"/>
                        </Style>
                    </ContextMenu.ItemContainerStyle>
                </ContextMenu>
            </Button.Resources>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="{DynamicResource Brush_AccentPrimary}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="14">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <Button Grid.Column="5" Content="−" Click="MinimizeButton_Click"
                Width="28" Height="28" Margin="0,0,8,0"
                BorderThickness="0" FontSize="18" FontWeight="Bold">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="{DynamicResource Brush_AccentSecondary}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="14">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>

        <!-- Close Button (Right) -->
        <Button Grid.Column="6" Content="✕" Click="CloseButton_Click"
                Width="28" Height="28" Padding="0,0,0,2"
                BorderThickness="0" FontSize="16" FontWeight="Bold">
             <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="{DynamicResource Brush_AccentSecondary}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="14">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF6B6B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>
